#include <iostream>

int main (void) {
    int a, b;
    std::cout << "Enter two numbers:" << std::endl;
    std::cin >> a >>b; 
    std::cout<<"The product of "<<a<<" and "<<b<<" is "<<a*b<<std::endl; 
    return 0;
}